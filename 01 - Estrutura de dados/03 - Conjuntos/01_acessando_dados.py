# Title: Acessando dados
# Para acessar dados de um conjunto, podemos converter o conjunto em uma lista, pois a lista é indexada, e assim podemos acessar os dados através do índice.

numeros = {1, 2, 3, 2}

numeros = list(numeros)

print(numeros[0])
